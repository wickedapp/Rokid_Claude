# 复制为 relay/.remote.env 并填真值(该真值文件已被 .gitignore 忽略)
# 也可以直接运行 ../start-remote.command，它会自动生成 ROKID_TOKEN。
# 中继握手密钥:足够长的随机串,例如 `openssl rand -hex 24`
ROKID_TOKEN=
# 可选:你的 ngrok 固定域名(不含协议),例如 abc.ngrok-free.dev；留空则使用临时 ngrok URL。
NGROK_DOMAIN=
